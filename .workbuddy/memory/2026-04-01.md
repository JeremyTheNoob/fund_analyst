# 2026-04-01 Daily Log

## 指数型-固收基金 5板块分析框架完成

### 新增文件
- `data_loader/idx_bond_loader.py`（~450行）：指数型-固收专用数据加载器
  - `calc_daily_tracking_deviation()` — 日偏离度分布（均值/P50/P90/P95/超阈值比例）
  - `estimate_duration_from_holdings()` — 从持仓名称推断久期（国债/政金/信用/城投）
  - `analyze_credit_alignment()` — 信用等级对齐（信用下沉检测）
  - `monitor_rebalance_loss()` — 调仓损耗监测（流动性冲击预警）
  - `load_y10y_technical_analysis()` — 10年国债技术分析（MA20/MA60/金叉死叉/高低点）
  - `estimate_portfolio_ytm()` — YTM估算（无风险利率+信用利差+久期溢价）
  - `analyze_coupon_coverage()` / `build_fee_erosion_model()` — 票息覆盖率+费率侵蚀

- `reporter/idx_bond_report_writer.py`（~520行）：5板块+利率专题报告生成器
  - Section 1: 基本信息（跟踪精度模型·日偏离度分布）
  - Section 2: 收益表现（收益对比·费用侵蚀模型·票息覆盖率）
  - Section 3: 深度分析（久期对照·信用等级对齐·信用下沉套利检测）
  - Section 4: 风险预警（调仓损耗监测·流动性冲击预警）
  - Section 5: 投资建议（YTM预期·适合人群·买卖时点·10年国债技术分析专题）

### 修改文件
- `pipeline.py` — 新增 `idx_bond` 额外数据加载块 + 报告生成路由
- `main.py` — 新增 `_render_idx_bond_report()` UI渲染函数 + 路由分发

### 关键修复
- 基准数据兼容：`bm_ret` 和 `close` 两种列名均支持
- Python 3.13 UnboundLocalError：循环变量 `ratio` 必须在循环外初始化
- `cm` 未定义：改用 `metrics.common.annualized_return`

### 新增图表标记
- `[INSERT_CHART: Y10Y_TREND]` — 10年国债收益率走势与均线

### 测试
- 161119（易方达中债新综指LOF A）端到端测试通过
- 5板块+利率专题完整生成，y10y_chart_df 可用

## 基金类型覆盖审计 + bond_long 利率专题

### 覆盖审计结果
- AkShare `fund_name_em` 全量扫描：32种类型、26,367只基金
- 已支持12种（23,341只，88.6%），已排除20种（3,018只，11.4%），未映射0种
- 修复：空字符串类型（8只）和未知类型从静默 fallback 改为 `unsupported`

### bond_long 利率专题
- `idx_bond_loader.py` 新增 `generate_y10y_rate_topic()` 共享函数
- `bond_report_writer.py` 新增 `section_rate` 板块（仅 bond_long 生成）
- `pipeline.py` 给 bond_long 加载 `y10y_chart_df`
- `main.py` `_get_bond_chart_fig` 新增 `Y10Y_TREND` 图表支持
- 内容：技术面均线系统 + 高低点转换 + 基本面驱动力 + 情绪面股债跷跷板
- 003838（广发聚利债券）测试通过，bond_mixed2 不受影响

## 数据获取与计算全链路审计完成

审计范围：13 种分析框架 × 数据加载 → 计算引擎 → 报告生成全链路。
- **数据加载层**：全部 19 个加载器就位，17 个 AkShare API 接口验证通过
- **计算引擎层**：4 个引擎（equity/bond/index/cb）输出与 Schema 一致
- **Pipeline 路由**：12 种框架 → 4 个引擎路由正确

### 发现 5 个问题
- **P1-1**: ~~`equity_engine.py` `_compute_style_box` 读取不存在的键 `beta_smb`/`beta_hml`（应为 `SMB`/`HML`）~~ ✅ 已修复
- **P1-2**: ~~`index_loader.py` `load_etf_holdings_ratios` 检查错误列名 `"资产类别"`（应为 `"资产类型"`）~~ ✅ 已修复
- **P1-3**: ~~`base_api.py` `_ak_bond_china_yield` 未实现，信用利差走兜底~~ ✅ 已修复：接入 `ak.bond_china_yield()` 接口，自动分年请求，返回宽格式（index=date, columns=期限_曲线名）
- **P2-1**: ~~bond_loader/equity_loader 年份硬编码 2024/2023/2022~~ ✅ 已修复：改为 `datetime.now().year` 动态生成最近3年
- **P2-2**: ~~`load_cb_holdings_with_details` 年份硬编码 2024~~ ✅ 已修复：同上

## 报告生成层审计完成

审计范围：`reporter/` 目录 14 个文件（13 writer + 1 translator），覆盖 13 种基金框架。

### 路由架构
- **简版**（translator.py）：按 `metrics` 字段路由，生成 `{headline, body, advice, risk_warning}` 4段式
- **深度版**（main.py）：按 `fund_type` 路由到对应 writer，生成 `{meta, headline, section1~N, full_text}`

### 13种框架报告结构总览
| 框架 | 文件 | 板块数 | 核心图表 |
|---|---|---|---|
| stock | equity_report_writer | 5 | CUM_RET, BRINSON, EXCESS_ALPH, DRAWDOWN |
| hybrid_equity/balanced | hybrid_equity_report_writer | 6 | ASSET_ALLOCATION, CUM_RET, HEATMAP, RBSA_DRIFT, DRAWDOWN |
| hybrid_flexible | hybrid_flexible_report_writer | 5 | CUM_RET, HEATMAP, TIMING_COMPARISON, DRAWDOWN, ROLLING_BETA |
| hybrid_absreturn | hybrid_absreturn_report_writer | 4 | CUM_RET, HEATMAP, VOLATILITY_BAND, DRAWDOWN |
| hybrid_bond | hybrid_bond_report_writer | 5 | CUM_RET, ASSET_ALLOCATION_PIE, BOND_HOLDINGS_PIE, DRAWDOWN |
| bond_long/short | bond_report_writer | 5+1 | CUM_RET, DRAWDOWN, HEATMAP, RATE_PREDICTION, Y10Y_TREND(long) |
| bond_short | bond_short_report_writer | 5 | BOND_HOLDINGS_PIE, CUM_RET, DRAWDOWN |
| bond_mixed2 | bond_mixed2_report_writer | 5 | CUM_RET, ASSET_ALLOCATION_PIE, DRAWDOWN |
| bond_mixed1 | bond_mixed1_report_writer | 5 | CUM_RET, ASSET_ALLOCATION_PIE, DRAWDOWN |
| idx_stock | idx_stock_report_writer | 5 | CUM_RET, PE_PERCENTILE, PB_PERCENTILE, TOP10_WEIGHTS, EXCESS_ALPH(增强) |
| idx_bond | idx_bond_report_writer | 5+1 | CUM_RET, DRAWDOWN, Y10Y_TREND |
| index(旧版) | index_report_writer | 4 | CUM_RET, TRACKING_ERROR_SCATTER, PREMIUM_DISCOUNT |
| cb | cb_report_writer | 4 | CUM_RET, CAPTURE_RATIO, DRAWDOWN |

### 审计发现
- ✅ 路由覆盖完整、返回结构统一、兜底机制完善
- ⚠️ equity/hybrid_equity 投资建议中「买入/持有/离场」子板块为占位符
- ⚠️ bond_mixed1/mixed2 缺少专属图表标记
- ⚠️ hybrid_absreturn 无独立投资建议板块
