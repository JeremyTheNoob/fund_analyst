# 2026-03-25 工作日志

## 2026-03-25 13:31 债基三因子模型全面升级（v10.0）

**问题**：债券基金（如000069）单因子T-Model R²=0.03极低，凸性失效，无法量化信用风险

**四大升级**：

### 1. 数据层：fetch_bond_three_factors()
- 短端因子(ΔY_2Y)：bond_zh_us_rate「中国国债收益率2年」（稳定每日更新）
- 长端因子(ΔY_10Y)：bond_zh_us_rate「中国国债收益率10年」（同上）
- 信用利差(ΔCS)：bond_china_yield 按自然年分段拉取（中短期票据AAA 3年 - 国债3年），各年分别请求后拼合

### 2. 模型层：run_duration_model()三因子升级
- 路径A（三因子）：R_p = α + β_short·(-ΔY_2Y) + β_long·(-ΔY_10Y) + β_credit·ΔCS + ε
- 路径B（单因子降级）：三因子数据不可用时自动降级到原T-Model，向后兼容
- 凸性改为_convexity_from_holdings()静态穿透法（回归法对日频不准）

**000069实测结果**：
- 单因子R²=0.03 → 三因子Adj.R²=0.0790（提升2.6倍）
- β_short=0.619(p=0.000)：短端资金面高度显著！（印证存单/短融特征）
- β_long=0.348(p=0.024)：长端也有一定敞口
- β_credit=-0.653(p=0.000)：信用下沉确认！利差扩大净值显著受损
- α(年化)=2.55%：扣除三因子后的carry（票息+骑乘+杠杆）

### 3. 展示层：三因子专属KPI卡（4列）
- 短端久期（2年期）+ 长端久期（10年期）+ 信用利差β + 年化carry
- 模型标签badge（绿=三因子/橙=单因子）+ Adj.R²展示
- 信用利差方向图（p<0.1时自动出现）

### 4. 压力测试：场景化矩阵（4场景）
- 场景A：资金面收紧（短端Y2↑50BP）
- 场景B：债市熊平（长端Y10↑50BP）
- 场景C：信用风险爆发（信用利差扩大20BP）
- 场景D：极端冲击（短端+长端同时↑100BP）
- 每个场景卡片含：短端/长端/信用三个分量拆解

---

**问题**：纯债/固收型基金（model_type='bond'）在基金类型卡片上错误显示「股票仓位 5%」

**根因**：仓位标签渲染逻辑的 `else` 分支同时覆盖了 bond 和 mixed 两种 model_type。`fetch_holdings` 中 bond 类型的默认 stock_ratio=0.05（行业经验默认值，代表现金替代头寸），这个5%不代表真实持股，但被渲染出来了。

**修复**：
- 新增 `elif model_type == 'bond'` 专属分支：`_stock_tag = ''`，只显示 `债券仓位 XX%`
- 混合型（else）：stock_ratio > 2% 才显示股票仓位 tag，避免极小值干扰

---

## v9.4 数据管线全面重构（10项关键修复）

**文件：`fund_analysis.py` = `fund_analysis_v9.4.py`**

### 修复1（致命）：Alpha量纲Bug
- **原因**：`_run_single_ff` 用标准化后的X回归截距作为Alpha，量纲严重失真（截距≠Alpha）
- **修复**：Alpha从**原始回归（m_raw）**截距取，标准化回归只用于横向Beta比较，Alpha不再为0

### 修复2（致命）：R²变量作用域Bug
- **原因**：`run_ff_model` 中R²突降检测（第992行）引用了`r2`变量，但该变量要到第1014行才赋值→NameError或用错值
- **修复**：提前从`res_full`取出所有结果变量，R²突降检测移到正确位置

### 修复3（高风险）：数据对齐"不满60天"根因
- **原因**：`run_ff_model`和`run_duration_model`都用`inner join + dropna()`，因子/国债数据有轻微缺口就会大量丢行
- **修复**：改为`left join（以基金净值为准）+ ffill(limit=3) + dropna(subset=核心列)`，错误信息加上原始行数/对齐后行数辅助诊断

### 修复4（高风险）：久期"忽零忽五"
- **原因1**：R²<0.3时回归不显著，但代码仍用噪音久期进压力测试
- **原因2**：数据不足时默认值硬编码5.0（偏大）
- **修复**：R²<0.3→回退到行业经验值2.5年（`fallback_low_r2`）；数据不足默认2.5年；加np.clip(0.1, 15.0)范围保护；压测展示加"（经验估算值）"标注

### 修复5（高风险）：基准变横线
- **原因**：`fetch_index_daily`的`pct_change()`首行永远是NaN，`dropna()`后第一行收益率丢失，`cumprod`时出现传播问题
- **修复**：`pct_change().fillna(0)`，首行填0不影响累计收益从1出发；`build_benchmark_ret`各出口都加`fillna(0)`防护

### 修复6：跨市场估值路由（港股/QDII）
- **修复**：加`_identify_market()`识别A股(6位)/港股(5位)/其他；港股用`stock_hk_valuation_baidu`；OTHER类标注"跨市场个股，估值数据暂缺"

### 修复7：残差UI优化
- "模型残差"→"选股择时及其他超额"
- 残差>0.5%时在饼图下方加说明卡，解释打新/日内交易等不可分解的合理超额

### 其他细节
- `bond_stress_test`展示区久期来源标注
- `run_duration_model`解读文本加回归质量提示
- 保留了v9.3所有功能（缓存/残差分析/合规补丁/累计收益图修复）

---

## v9.5 全面代码审查修复（8项潜在Bug）

**文件：`fund_analysis.py` = `fund_analysis_v9.5.py`**

### Bug1（高风险）：bond模式translate_results/calc_radar_scores数据路径错误
- **根因**：bond模式下`model_results={'bond': bond_res}`，但translate_results/calc_radar_scores直接`results.get('duration')`取不到数据，全部返回默认值
- **修复**：调用前检测bond模式并展开子字典：`_translate_results = model_results['bond']`

### Bug2（高风险）：fetch_ff_factors内部inner join截断因子数据
- **根因**：构建FF因子矩阵时，small/val/grw与mkt做inner join，某个指数停牌一天会截断所有行
- **修复**：改为left join + ffill(limit=3)，以沪深300日期为基准

### Bug3（中风险）：_empty_ff_result缺少r_squared_recent和residual_insight字段
- **根因**：展示层读这两个字段，但_empty_ff_result未返回，导致KeyError或AttributeError
- **修复**：补全两个字段（None/''}）

### Bug4（中风险）：estimate_hidden_cost的type_category永远是equity
- **根因**：调用时未传入type_category，函数签名无此参数，所有基金都用equity隐性费率0.15%
- **修复**：函数签名加type_category参数，调用处传入`basic['type_category']`

### Bug5（中风险）：run_brinson使用dir()判断变量存在（Python反模式）
- **根因**：`r_bm_ann if 'r_bm_ann' in dir() else 0.0`，dir()是类方法列表，不是本地变量检测
- **修复**：改为直接引用（两条分支均保证变量被赋值）

### Bug6（中风险）：fetch_bond_index首行NaN未填0
- **根因**：与fetch_index_daily不一致，bond_index用dropna()丢掉了首行
- **修复**：pct_change().fillna(0)，去掉dropna()

### Bug7（低风险）：sector基金估值预警右列在bond_ratio<=0.10时不渲染
- **根因**：_has_right_content条件未检查top10是否有数据
- **修复**：加持仓数据非空检测

### Bug8（低风险）：calc_radar_scores的bond模式也有嵌套字典问题
- **根因**：与Bug1同源，calc_radar_scores传入{'bond':bond_res}
- **修复**：同样展开bond子字典

---

## 显性费率永远为0的根本原因修复

**问题**：费率卡片显示管理费/托管费/显性合计均为0
**根因**：
1. 雪球接口 `fund_individual_basic_info_xq` 返回的字段里根本没有「管理费率」「托管费率」「销售服务费率」（实测14个字段只有基金基本信息）
2. 天天基金 `fund_open_fund_info_em(indicator='基金概况')` 返回空 DataFrame（indicator参数不支持此值）
3. 两层数据源都取不到费率，所以 fee_manage/fee_custody/fee_sale 永远是初始值 0.0

**修复**：
- 主方案：改用 `ak.fund_fee_em(symbol, indicator='运作费用')`，返回格式为固定列序：col1=管理费率值, col3=托管费率值, col5=销售服务费率值
- 兜底方案：`ak.fund_individual_detail_info_xq(symbol)` 读「其他费用」行，含「基金管理费」和「基金托管费」（值为 float 百分比数字，加%后交_parse_fee解析）
- 实测：000001 管理费1.20% 托管费0.20% 合计1.40%；000961 管理费0.50% 托管费0.10% 合计0.60%，读取正确

---

## 2026-03-25 10:03 任务恢复确认

**当前状态**：
- `fund_analysis.py` = `fund_analysis_v9.6.py`（完全一致，0 diff）
- v9.6 本地 commit 已完成（hash: 48b7b64）
- **待完成**：`git push origin main`（GitHub HTTPS 需要 token，SSH key 不存在）
- remote: `https://github.com/JeremyTheNoob/fund_analyst.git`

**需要用户操作**：提供 GitHub Personal Access Token 或手动在终端执行 `git push origin main`

---

## 2026-03-25 10:11 数据源讨论结论

**结论：维持 AkShare 为主，暂不切换 Tushare**
- 净值/持仓数据两者来源头相同，实质质量差异小
- Tushare 优势：稳定性略好、持仓历史更深；劣势：200元/年、中债指数缺失、PE分位需自算
- 模型质量真正天花板是 FF 因子构建方式（代理指数 vs 真正截面因子），两个数据源都解决不了
- **分阶段策略**：现阶段维持 AkShare → 有付费用户后双源兜底 → 规模化后升级因子构建

**下一步**：用户去实际跑测试，覆盖边界场景（纯债/行业ETF/港股持仓/次新基金/特殊基准格式），发现问题后继续迭代

---

## 2026-03-25 10:46 v9.7 多类型兼容修复（基于实测截图问题清单）

**文件：`fund_analysis.py` = `fund_analysis_v9.7.py`**

**测试发现的问题（5种基金）**：
- 000069（纯债型）：凸性0.0误报「正凸性」、久期解读重复、雷达分与大白话分不一致
- 457001（QDII混合持仓）：因子数据获取失败（sector分支无法处理）、业绩基准线缺失、持仓中台积电/英伟达N/A
- 515880/515050/512040（ETF）：基金信息获取失败
- 024709/026807（次新基金）：基金信息获取失败

**修复清单（10项）**：
1. [致命] ETF识别失败 → `fetch_basic_info` 加 `fund_name_em()` 兜底，515880/512040/024709均可识别
2. [致命] QDII走sector分支→申万行业指数拉不到→报错 → 新增 `qdii` type_category，走 equity 模型 + 跨市场警告
3. [高] 次新基金净值不足60天直接退出 → 改为警告，20天以上可展示基础分析
4. [高] 百度估值接口偶发503 → 加探测机制，整体故障时所有持仓快速返回「接口维护中」
5. [中] 台股/韩股(TSM/NVDA/005930/009150KS等)被误识别为港股 → 完善正则，英文ticker/后缀归 OTHER
6. [中] 港股PE历史只有近一年（默认）→ 加 period 参数，匹配 A股 period 参数
7. [中] QDII无MSCI基准导致图表出错 → 用空 bm_df，不用沪深300伪基准
8. [中] 债券凸性0.0被说成「偏低/负」→ `|convexity|<2` 改提示「样本内利率波动不足」
9. [中] 久期 fallback 文字重复（说了两遍「已回退」） → 合并提示，去重
10. [中] 雷达图均分≠大白话评分 → Part 4 综合分直接复用雷达图 `_avg_r` 变量

**commit**: b0c70e1（本地已提交，push 因网络问题待重试）

---

## 2026-03-25 13:04 QDII基金4个专项修复

**测试基金**：某QDII亚洲基金（含台积电/阿里/京东/康耐特光学/汇聚科技等境外持仓）

**问题1：累计收益图无基准线**
- 根因：QDII基金基准解析为空`{}`，bm_df为空DataFrame，图中只有基金净值，底部说明也没有针对QDII的解释
- 修复：新增`_is_qdii_fund`变量（`basic['type_category'] == 'qdii'`），bm_df为空时显示「境外指数暂无法自动获取」+附investing.com链接供手动对比

**问题2：因子模型显示「因子数据获取失败」**
- 根因：QDII走equity模型分支，展示层只判断`_is_new_fund`，不判断QDII；A股FF因子对境外持仓无意义，返回N/A
- 修复：在`_is_qdii_fund`为True时，展示QDII专属卡片（4条建议：夏普比率/最大回撤/汇率风险/MSCI基准对比），不再显示通用「因子数据获取失败」

**问题3：前十大重仓股大量N/A（应为跨市场）**
- 根因：
  - 台股/美股ADR（TSM/NVDA/PDD）→ 识别为OTHER → `risk_level='跨市场'`（正确）
  - 港股（如09988阿里）→ 识别为HK → 百度港股接口失败 → 保持初始化值`risk_level='数据缺失'`，显示N/A
- 修复：港股百度接口未返回数据时，新增`risk_level='港股暂缺'`+说明「请在港交所官网查询」
- 表格展示：跨市场/港股暂缺的PE/分位/PB统一显示「—」代替N/A（语义更清晰）

**问题4：QDII持仓说明**
- 修复：估值表上方加说明「🌐 该QDII基金含X只境外股票，当前接口仅支持A股和部分港股估值分析」

---

## 2026-03-25 12:58 次新基金3个专项修复

**测试基金**：中金精选股票A（2025-06-30成立，约180天数据）

**问题1：「未知公司」**
- 根因：`_known_companies` 列表缺少「中金」及大量中字头券商系、其他中小公司
- 修复：新增「中金/中信建投/中信保诚/中邮/中融/中海/中航」及「汇泉/睿远/东证/安信/方正富邦/德邦/国投瑞银/申万菱信/民生加银/金鹰/上银」共17个

**问题2：「未获取到业绩基准」**
- 根因：bm_text没做默认值映射，直接显示「未获取到业绩基准」
- 修复：新增 `_default_bm_names` 字典，按type_category返回「沪深300（默认）/ 中债综合指数（默认）/ 沪深300 60%+中债 40%（默认）」，让用户知道是推断值而非招募说明书原文

**问题3：股票仓位61%（次新基金建仓期）**
- 根因：建仓期（3-6个月）仓位本来就不会满，但标签没有说明，用户以为是保守操作
- 修复：equity类型+次新基金（`_is_new_fund`）+股票仓位<75%→显示橙色「建仓中 61%」标签（tooltip：次新基金通常有3-6个月建仓期）

**顺带：「因子数据获取失败」改为友好提示**
- 次新基金因子模型失败时，新增专属卡片：「⏳ 次新基金因子模型受限：净值历史仅X天，需至少60个交易日。建议X个交易日后再次分析。」

---

## 2026-03-25 12:50 新增2个ETF专项修复

**问题1：指数ETF显示「股票仓位67%（行业集中型）」**
- 根因：ETF季报申报仓位（含5-10%备付金）不能代表「满仓跟踪」的策略意图；且67%可能是某季报实际值，与「行业集中型」语义冲突
- 修复：新增 `_is_index_fund` 判断（ETF或type_raw含「指数」），区分「ETF行业指数型」vs「主动行业型」
  - ETF：显示「满仓跟踪指数」主标签 + 「季报股票仓位67%（XXXX季报）」次标签（加tooltip说明备付金）
  - 主动行业型：保持「股票仓位X%（行业集中型）」

**问题2：ETF规模显示N/A**
- 根因：雪球接口对ETF返回KeyError:'data'，兜底逻辑只处理了成立日和经理，没有规模
- 修复：新增 `fund_scale_open_sina()` 接口调用（新浪开放式基金规模表），包含：最近总份额+单位净值+成立日期+基金经理
  - 规模计算：最近总份额 × 单位净值 ÷ 1e8，自动判断显示「X.X亿元」或「X百万元」
  - 同时补全成立日期（如2019-08-16）和基金经理（如艾小军）
  - 实测515880 = 151.5亿元（143.89亿份 × 1.0526）
- 触发条件：`_need_scale or _need_manager or _need_estdate` 任一为真均触发，不限ETF

---

## 2026-03-25 11:46 新增4个问题修复

**雷达图小字固定 → 动态定性评价**
- 新增 `_score_label(score, dim)` 函数，按0-100分范围返回差/一般/良好/优秀的文字评价（共4档8×5=40条文案）
- 每个维度卡片的小字不再是"净值曲线的稳定性..."这类固定描述，而是动态反映得分含义

**「有选股能力但总分低」困惑**
- 综合均分后新增`_avg_comment`解释：总分<55分时，指出最低维度名称+分数，说明"某维度严重拖低了均分"
- 例：风险控制10分会触发「「风险控制」仅10分严重拖低了均分——能赚钱但赚得险」

**「未知公司」修复**
- 雪球接口对ETF返回company字段为空，新增从基金名称前缀匹配公司名（国泰→国泰基金等）
- 收录35个常见基金公司

**数据波动原因**（+215% vs +225%）
- 调查结论：数据本身正常，两次测试中间有几天净值更新导致总收益略有变化
- 代码确认使用累计净值（v9.2已修复），非bug

---

## 2026-03-25 11:40 新增2个问题修复

**测试基金**：515880（行业/主题型ETF）

**问题1：仓位标签不合理（股票仓位67%+债券仓位5%，28%不明去处）**
- 根因：所有类型统一显示股票仓位+债券仓位两个tag，行业型基金主要关注行业Alpha不是仓位比例，显示两个碎仓位数字反而让用户困惑
- 修复：sector类型只显示「股票仓位X%（行业集中型）」，不显示债券仓位；equity类型同理不显债券；只有混合型才显两个

**问题2：选股能力表述矛盾**
- 根因：表格用`na>0.03`即显示「✅具备选股能力」；性格诊断用`ir>0.5`区分「行业随从」vs「精准潜水员」，两套标准不一致（515880 na=0.044>0.03但ir=0.44<0.5，两边判断相反）
- 修复：统一标准：`na>0.03 AND ir>0.5` 才说「具备选股能力✅」；`na>0.03 BUT ir≤0.5` 显示「🟡微弱Alpha，效率不足」，并在实力诊断里说「主要靠行业Beta」，与性格诊断的「行业随从」保持一致

---

## 2026-03-25 11:21 本地测试发现2个新问题并修复

**测试基金**：515880（国泰中证全指通信设备ETF，指数型-行业/主题型）

**问题1：ETF基本信息全N/A（成立时间/基金经理/规模）**
- 根因：雪球接口对ETF返回KeyError:'data'，fund_name_em只有名称和类型，没有成立日/经理/规模
- 修复：加第3层兜底——对`_is_etf`判断，用`fund_open_fund_info_em(indicator='单位净值走势')`的最早日期推断成立日，基金经理显示「被动跟踪（指数型）」
- ETF规模暂无稳定AkShare接口（fund_etf_spot_em连接不稳），留N/A

**问题2：收益拆解总超额收益显示0.00%**
- 根因1：`run_sector_model`返回字典无`excess_return`字段，`performance_decomposition`取到0
- 修复：`run_sector_model`返回时加`'excess_return': neutral_alpha`
- 根因2：`performance_decomposition`对sector类型，`_mr = model_results`（本体）而不是`sector_results`子字典
- 修复：`if sector_results`时将`_mr = sector_results`；`total_excess`优先取`neutral_alpha`

**问题3：收益拆解标题「这+215%是怎么赚的」混淆绝对收益和超额收益**

## 2026-03-25 14:00 债券持仓穿透分析（v10.1）

**背景**：`fund_portfolio_bond_hold_em` 接口可返回季报全部债券持仓明细（不像股票只有前十大），000069 返回53只债券，有债券代码/名称/占净值比例。

**新增功能**：
1. `fetch_holdings` 新增 `bond_holdings` 字段：bond/mixed 类型自动拉取 `fund_portfolio_bond_hold_em` 全部持仓
2. `_classify_bond()` + `_bond_credit_tier()`：从名称+代码识别9种债券类型及信用层级
3. `analyze_bond_structure()`：穿透引擎
   - 可转债精确查询 `bond_zh_cov_info` 获取评级/到期日/票面利率（实测 AAA 杭银转债、AA 爱玛转债等）
   - 其他债券启发式估算剩余期限（按名称发行年份+类型经验值）
   - 输出 rate_ratio / credit_ratio / convert_ratio
4. 展示层「🔍 债券持仓穿透」板块：Plotly 甜甜圈图（类型分布）+ 可转债评级 Bar + 明细折叠表 + 三因子联动解读
   - 与 β_credit 联动：季报信用债占比 → 印证三因子的信用下沉结论

**验证**：000069 实测 - 政策性金融债 51.1% / 可转债 24.8% / 国债 13.3%；可转债评级查询全通

**文件**：`fund_analysis.py`（已 commit b57cd92，待手动 push）

## 2026-03-25 14:14 凸性穿透修复

**Bug**：深度量化板块一直显示「凸性：受限于季报数据无债券类别明细，无法精确穿透」

**根因**：`_convexity_from_holdings()` 读取 `holdings['gov_bond_ratio']` 和 `holdings['credit_bond_ratio']`，但这两个字段此前从未被赋值（只有注释写「未来扩展」），永远走 else 分支输出旧提示。

**修复**：
1. 主流程调用 `run_duration_model` 前，注入穿透数据：
   - `holdings['gov_bond_ratio'] = rate_ratio × total_weight / 100`
   - `holdings['credit_bond_ratio'] = (credit+convert)_ratio × total_weight / 100`
2. `_convexity_from_holdings` 判断逻辑升级：有穿透数据时给出精确评级（strong/weak/moderate）

000069 实测：利率债 64.4% → 凸性评级 **strong**，文案「凸性：强（季报穿透，利率债占持仓64%）」

commit: 6974f9e，待 push
- 修复：标题改为「超额收益拆解：基准以上+X.XX%」，后面小字注明「区间总收益215%，拆解为相对基准超额」

## 2026-03-25 14:xx 深度量化板块文字全面白话化（v10.2）

**用户需求**：第二部分"深度量化"的文字太专业，要精简白话化。

**改动清单**：
1. **fund_type_note**：「短端资金面/存单/短融策略」→「主要买短期债，利率一动净值就跟着动」
2. **credit_note**：「β_credit=xxx/利差扩大净值显著受损」→「信用风险扩大时净值会跌，高收益对应信用风险」
3. **interpretation parts**（三因子路径）：「组合有效久期/对利率变动不敏感/年化综合carry/Adj.R²」→「利率敏感度高/低/收益来源估算」
4. **interpretation parts**（单因子路径）：同上
5. **_convexity_from_holdings**：「正凸性保护好/利率大幅下行时净值加速上涨」→简化为「利率大跌时净值涨得更快，下行保护好」
6. **展示层-信用利差方向图**：「信用下沉型/利差收窄获益」→「持有信用债/信用风险扩大净值会跌」
7. **KPI卡标签**：「短端久期(2年期)/长端久期(10年期)/信用利差β/年化综合carry」→「短债敏感度/长债敏感度/信用风险敞口/年化收益来源」
8. **模型标签**：「三因子期限结构归因」→「利率+信用三维分析」
9. **底部说明**：数学公式「Rₚ = α + β_short·(-ΔY_2Y)...」→「将净值涨跌对三个因子做回归」
10. **_explain_row**：凸性/年化信用溢价解释适度简化
11. **大段综合解读**：由分号连接的长段文字改为 bullet 列表，去掉凸性重复行

commit: dced4d9，待 push

## 2026-03-25 14:53 权益基金两大Bug修复（v10.3）

**问题现象**：分析 001875（前海开源沪港深优势精选）时，走势图无基准线 + 因子数据获取失败。

**根本原因**：
1. `stock_zh_index_daily_em` 接口已挂（返回空），FF因子所有指数全部拉不到
2. 001875基准含「恒生指数×35%」，但 `_INDEX_NAME_CODE` 里 `恒生: None`，走银行活期0收益兜底，导致基准失真或缺失

**修复内容**：
1. `fetch_index_daily`：主力切换为 `stock_zh_index_daily`，备用保留 `stock_zh_index_daily_em`（双接口降级）
2. `_INDEX_NAME_CODE`：恒生/恒生指数/恒生科技/恒生国企 改为 `hk:HSI/hk:HSTECH/hk:HSCEI`
3. 新增 `fetch_hk_index_daily(sina_symbol, start, end)`：用 `stock_hk_index_daily_sina` 获取港股指数
4. `build_benchmark_ret`：识别 `hk:` 前缀，调用 `fetch_hk_index_daily` 而非 `fetch_index_daily`

**影响**：所有权益基金的FF因子分析恢复正常；含港股基准的沪港深/QDII基金基准线也恢复正常

commit: 09f045c，待 push

## 2026-03-25 15:17 性能全面优化（v10.4）

**问题**：每次分析一支基金运行特别慢，10秒以上。

**根本原因分析**：
1. `fund_name_em()` + `fund_scale_open_sina()` 在 `fetch_basic_info` 内部直接调用，**无缓存**，每次分析都重拉几千行全量表（各1-3秒）
2. `fetch_ff_factors` 内部4个指数**串行**拉取（4×1秒=4秒）
3. STEP3持仓 + STEP4基准构建**串行**（各1-2秒）
4. `fetch_bond_three_factors` 内部5年数据**串行** for 循环（5×0.5秒=2.5秒）
5. `retry_on_failure` 默认 delay=2s，接口偶发失败时白等2秒

**优化内容**（commit: eaf31e4）：
- `fund_name_em/fund_scale_open_sina` 封装为 `_get_fund_name_list()/_get_fund_scale_sina()`，**全天86400s缓存**，同一天内所有基金共享，不重复拉
- `fetch_ff_factors`：4个指数改为 `ThreadPoolExecutor(max_workers=4)` 并行拉取
- STEP3+4：`fetch_holdings` + `build_benchmark_ret` 改为并行执行
- 债券模型：`fetch_treasury_10y` + `fetch_bond_three_factors` 并行
- `fetch_bond_three_factors` 内部按年分段改为 `ThreadPoolExecutor` 并行
- `retry_on_failure` 默认 delay 从 2s→1s

**预期效果**：首次分析从约15秒→5秒以内；缓存命中后（同一天第二次）几乎秒开
