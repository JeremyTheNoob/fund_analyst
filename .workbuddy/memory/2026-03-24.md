# 2026-03-24 工作日志

## v9.3 补丁：累计收益图三项修复（2026-03-24 深夜+）

### Bug 1：两条线起点不对齐（所有类型）
- **原因**：`plot_cumulative_return` 里基金和基准各自独立 `cumprod`，日期不做 inner join，起点可能相差数天 → 基准曲线在图上从偏移位置出发，造成视觉失真
- **修复**：改为先 inner join 对齐日期，再共同 `cumprod`，保证两条线都从同一天 0% 出发

### Bug 2：行业基金累计图用沪深300当基准（sector 类型）
- **原因**：STEP 4 默认基准对 sector 类型设置为沪深300；虽然 `run_sector_model` 内部用了申万行业指数做 Alpha 计算，但累计收益图一直画的是沪深300
- **修复**：STEP 5 sector 分支成功拉取申万行业指数后，用 `sw_ret` 覆写 `bm_df`；同时更新 `_sector_bm_label` 字符串，图例和小字说明一并更新为"申万XX行业指数（精准行业基准）"

### Bug 3：`bm_text` 赋值顺序问题
- **修复**：展示区统一用 `_sector_bm_label if _sector_bm_label else basic['benchmark_text']` 决定最终基准文本

---

## v9.3 四项实战优化（2026-03-24 深夜）

**4项改动，已全部落地，文件：`fund_analysis.py` = `fund_analysis_v9.3.py`**

### 第1条：本地数据缓存池（Speed & Stability）
- `fetch_index_daily` / `fetch_ff_factors` / `fetch_treasury_10y` / `fetch_bond_index` / `fetch_sw_industry_ret`
- 全部从 `ttl=3600`（1小时）升级为 `ttl=86400`（24小时全天缓存）
- 理由：公共因子/国债/申万行业数据全市场通用，每日仅需拉取一次，当天所有用户命中缓存；基金特有净值/持仓仍保持实时拉取

### 第2条：前视偏差标注（Look-ahead Bias）
- 累计收益曲线下方业绩基准小字追加：「⚠️ 基于当前公开基准回溯，历史基准变更期间数据仅供参考」
- 鼠标悬停 title 属性提供详细解释

### 第3条：残差分析 · 神秘超额提示
- `run_ff_model()` 新增近期R²计算（`r_squared_recent`）和突降检测（`residual_insight`）
- 触发规则：全期R² - 近半年R² > 0.25 → 橙色预警卡「疑有非标资产收益或极端个股偏离」
- 全期R² < 0.4 → 蓝色信息「风格高度独立，常规因子分析仅供参考」
- 全期R² > 0.85 → 蓝色「高度贴合市场因子，需结合Alpha显著性判断」
- 展示位置：Part 2 权益量化解读点评卡片下方

### 第4条：法律免责声明（Compliance）
- 替换原有简单 caption 为完整合规补丁卡片
- 五要素：不构成投资建议 / 模型局限 / 前视偏差说明 / 数据来源 / 风险提示

---

## v9.2 数据层全面修复（2026-03-24 深夜）

**7项修复，已全部落地，文件：`fund_analysis.py` = `fund_analysis_v9.2.py`**

### 致命错误修复
- **F1**：fetch_nav 换用累计净值（indicator="累计净值走势"），修复分红跳空导致Alpha/Beta/回撤畸变
- **F2**：FF因子 RMW NaN→列级降维（>50%缺失直接drop），MOM重命名为Short_MOM

### 高风险接口修复
- **F3**：国债收益率接口顺序调换（bond_zh_us_rate 升主，bond_china_yield 降备），统一加 ffill()
- **F4**：估值预警新股/亏损股双拦截（<250条=次新股，PE≤0=亏损期降级用PB），加note字段

### 逻辑优化
- **F5**：holdings 新增0层 fund_portfolio_asset_allocation_em；自适应系数替换固定1.4倍
- **F6**：隐性费率废弃负自相关玄学估算，改用行业经验中位数（权益0.15%/债券0.03%）

### 工程加固
- **F7**：全局 retry_on_failure 装饰器（3次重试，间隔2s）

---

## v9.1 UI 全面优化（2026-03-24 晚）

**8项 UI 改进，已全部落地，文件：`fund_analysis.py` = `fund_analysis_v9.1.py`**

### 架构重组
- **Part 3 提前**：累计收益曲线从 Part 2.5 之后提前到 Part 1 之后（新命名为 Part 1.5「业绩走势一览」）
- **去掉 expander**：收益曲线直接 st.plotly_chart 铺满全宽，不再用 expander 包裹

### Part 2.5 动态布局
- 预判左列/右列是否有内容，用 `_has_left_content` / `_has_right_content` 决定双列或单列布局
- 无内容一侧不渲染，避免 UI 空白块

### 动态标题
- 收益拆解标题改为 f-string：`这 +X.XX% 是怎么赚的？` / `这 -X.XX% 是怎么亏的？`
- 涨红跌绿颜色变化

### 颜色语义化
- 雷达图和进度条阈值统一为：≥80绿 / 60-79橙 / <60红
- 进度条 border-left 颜色改为跟随分数（原为固定五色）

### Part 4 定性/定量分离
- risk 字段改为只输出定性裁决（如「满仓运作但无Alpha保护」），不重复 Part 2.5 的具体数字
- 末尾加引导语「详细数据见上方风险提示板块」

### 微文案提示
- 权益基金触发债券模型时，在 Part 2 债券结果前显示蓝色「💡 跨界扫描发现」提示框

### 移动端 CSS
- 久期压力测试三场景卡片从 `grid-template-columns:1fr 1fr 1fr` 改为 Flexbox + `min-width:120px`，手机端自动折行

## v9.0 风险提示板块新增（2026-03-24 20:50）

**新增三大功能，统一放在 Part 2.5「风险提示」板块（在 Part 3 业绩可视化之前）**：

### 功能一：收益拆解「功劳簿」
- 函数：`performance_decomposition(model_results, sector_results, nav_df, bm_df)`
- 三层拆分：仓位择时贡献（Brinson配置效应）/ 行业选股贡献（中性化Alpha×权重）/ 模型残差
- 精确模式（有行业权重）vs 估算模式（Alpha×0.7）vs 近似模式（Brinson选择效应）
- 输出：一句话叙事 + CSS 进度条三段图 + 功劳簿文字列表
- 触发条件：model_type in ('mixed', 'sector')

### 功能二：前十大重仓股估值风险预警
- 函数：`fetch_stock_valuation_alert(stock_codes, period='近五年')`（带@st.cache_data缓存）
- API：`ak.stock_zh_valuation_baidu(symbol, indicator='市盈率(TTM)', period='近五年')`
  - 返回 `{'date', 'value'}` 日频历史序列，支持'近一年'/'近三年'/'近五年'
  - 本地验证：600519当前PE=19.6，历史分位=2%（近5年历史低位）
- PE历史分位风险分级：≥85%→极度高估(🔴) / ≥70%→偏高(🟠) / ≤15%→极度低估(🟢) / ≤30%→相对低估(🔵)
- 同时获取PB数据作为备用（当PE无效如亏损股时）
- 触发条件：所有持仓股票仓位>15%的基金类型
- 辅助函数：`plot_valuation_alert_chart()` - 水平条形图 + 85%/15%警戒线

### 功能三：债券久期压力测试
- 函数：`bond_stress_test(eff_duration, bond_weight, bp_scenarios=[10,50,100])`
- 三场景：利率上行10BP / 50BP / 100BP
- 公式：ΔPrice ≈ −Duration × ΔY × 债券仓位
- 测试案例：久期6年+仓位35% → 100BP升息净值冲击-2.1%（🟠较高风险）
- 触发条件：债券仓位>10%（bond型+mixed型均触发）
- 股债双杀预警：股票仓位>40%且100BP利率冲击>1.5%时，显示专项警告

### 展示层逻辑
- 权益型：左列显示前十大估值预警 / 右列债券压测（低仓位则静默）
- 混合型：左列=收益拆解功劳簿 / 右列=估值预警（股仓>15%）+债券压测（债仓>10%）
- 行业型：左列=收益拆解功劳簿 / 右列=估值预警+（无债券压测）
- 债券型：左列静默 / 右列=债券压测

**备份：`fund_analysis_v9.0.py`**

## v8.8 翻译层全面升级（2026-03-24 19:45）

**本次修改（翻译层 Translate Matrix 全面重构）**：

1. **新增辅助函数 `_calc_rolling_alpha_trend()`**：
   - 对基金收益率 vs 基准做5个月滚动超额，步长21天
   - 判断最近3个月是否连续下降（slump）/上升（recovering）/震荡（volatile）
   - 返回 `trend_text`（直接可用的大白话解释）

2. **translate_results() 签名升级**：
   - 新增参数：`rolling_df`（仓位检测）、`bm_ret_for_trend`、`fund_ret_for_trend`
   - 返回字段新增：`tags`（性格标签列表）、`emotion_note`（情绪文本）、`consistency_warn`（一致性预警）

3. **equity 翻译层**：
   - **性格标签体系重构（8类）**：市场捕手/激进放大镜/稳健老兵/防御专家/指数影子/明星选股手/努力型选手/随波逐流型
   - 附加标签：小盘偏好/大盘偏好/价值风格/成长风格/追势动量/逆势反转
   - **一致性判定（新增）**：beta_20d>85%且alpha不显著→「无效加杠杆」预警；beta<30%且alpha显著→「轻仓高精准」观察
   - **情绪指标（新增）**：调用 `_calc_rolling_alpha_trend`，连续3月下降→「审美疲劳期」；连续上升→「状态好转」
   - 评分：情绪低迷时从80→72（小幅降分）

4. **bond 翻译层（三分类重构）**：
   - 防御专家（短久期+合理carry）/收益挖掘机（中长久期+高carry）/利率博弈师（长久期）/纯利率型/均衡债基
   - 附加标签：正凸性保护/负凸性风险/信用下沉
   - 建议+评分细化为4档

5. **mixed 翻译层（四象限重构）**：
   - 择时达人（配置为主+选股辅助）/选股匠人（选股为主+配置辅助）/择时驱动型/逆境选股型/两头挨打型
   - 评分：双轮驱动78/单轮驱动65/两头挨打42

6. **sector 翻译层（Alpha×IR四象限）**：
   - 赛道霸主（高Alpha+高IR）/单押赌博型（高Alpha+低IR）/精准潜水员（低Alpha+高IR）/行业随从（低Alpha+低IR）
   - **性价比预警（新增）**：高Alpha但IR低→"单押某几只个股，赢了是神输了是坑"
   - 单押赌博型评分62（低于赛道霸主82），建议控仓≤10%

7. **展示层 Part 4 升级**：
   - 评分旁边新增性格标签气泡（蓝色圆角标签）
   - 情绪指标卡片（绿/橙/灰，自动根据趋势类型）
   - 一致性分析卡片（警告/信息）
   - 两者均在四维诊断之前展示

**备份**：`fund_analysis_v8.8.py`



**本次修改（M4 行业/主题基金 Alpha 分析全面升级）**：

1. **申万行业指数数据层（新增）**：
   - `SW_INDUSTRY_MAP`：31个申万一级行业代码表（801010-801980）
   - `FUND_NAME_TO_SW`：基金名称关键词→行业代码映射表（40+关键词）
   - `fetch_sw_industry_ret(sw_code, start, end)`：调用 `ak.index_hist_sw` 获取日收益率，`@st.cache_data`缓存
   - `detect_sw_industry(fund_name, sector_weights)`：双重识别策略
     * 策略1：季报持仓行业权重（第一大行业>30%）
     * 策略2：基金名称关键词匹配
     * 返回 `(sw_code, industry_name)` tuple

2. **`run_sector_model()` 升级（精准行业基准）**：
   - 新增参数：`sw_industry_ret`, `sw_industry_name`, `fund_name`
   - 优先级：申万行业指数 > 招募说明书基准
   - 同时计算 vs 申万行业 Alpha 和 vs 沪深300 Alpha，做差值对比（基准陷阱检测）
   - 返回新增字段：`neutral_alpha_bm`（vs沪深300）, `bm_source`（基准来源说明）, `excess_series`（超额序列）, `sw_name`

3. **STEP 5 sector 分支升级**：
   - 自动调用 `detect_sw_industry` 识别行业
   - 如识别成功则获取申万指数并传入 `run_sector_model`
   - 显示 spinner 提示："获取申万X行业指数（精准基准）..."

4. **展示看板全面升级（行业 Alpha 诊断看板）**：
   - 基准来源标签：绿色（精准行业基准已启用）/ 橙色（未匹配提示）
   - 4行诊断表：行业内Alpha / 跟踪误差 / 信息比率 / 基准陷阱对比（差>2%时才显示）
   - 每行含：数值 / 状态emoji / 大白话解读
   - 景气/主题/动态基金特殊提示卡片
   - 指标释义折叠区（含申万基准说明）

5. **translate_results sector 分支升级**：
   - 区分：增强指数型（TE<4%）/ 主动型 / 高集中押注型（TE>15%）
   - 四维诊断（性格/实力/风险/建议）全部基于申万行业数据
   - 评分：na>5%且IR>1→82分；na>3%且IR>0.5→72分；na>0→58分；否则42分

6. **核心量化逻辑**（用户指定）：
   - `annual_alpha = excess.mean() * 252`
   - `tracking_error = excess.std() * np.sqrt(252)`
   - `IR = annual_alpha / tracking_error`
   - IR>1.5→高效Alpha / 0.5~1.5→效率尚可 / <0.5→性价比偏低

**备份**：`fund_analysis_v8.7.py`



## v7.0 全新重写

**背景**：v6.x系列因DataFrame列名重复等Bug持续崩溃，决定从架构层面重写。

**v7.0 架构设计**：
- 文件：`fund_analysis_v7.py` → `fund_analysis.py`
- 代码量：约860行（v6 的1.4倍，但逻辑更清晰）
- 5阶段分析框架，每个阶段独立函数

**关键改进**：
1. DataFrame构建全部用 `pd.DataFrame({'列名': [...]})` 字典方式，彻底避免转置导致的列名重复
2. 所有 `st.dataframe()` 调用都使用干净的DataFrame，无隐藏属性（_errors等）
3. `safe_float()` 统一处理所有数值转换，避免崩溃
4. 三类基金（权益/债券/指数ETF）完全分支处理，不混用
5. 多接口容错：雪球 → 天天基金 → 日报接口依次尝试

**新功能（vs v6）**：
- 债券类：Sortino比率、信用分层（AAA/AA占比）、可转债识别、收益构成分析
- 权益类：加权PE/PB、HHI集中度、晨星风格箱推断
- 诊断报告：四维诊断（性格/实力/风险/避坑）+ 综合评分
- UI：Tab布局（量化看板/持仓穿透/图表/诊断报告）

## v7.1 修复CSS TokenError

**问题**：Streamlit Cloud 部署后报错 `tokenize.TokenError`，错误指向第88行 `.tag-red { background: ... }`

**原因**：CSS 字符串直接放在模块顶层，被 Python 的源码解析器（用于 `@st.cache_data` 装饰器缓存 key 生成）误读，大括号 `{}` 和点号 `.` 被当作 Python 语法解析导致崩溃

**修复**：将 CSS 字符串移到 `render_css()` 函数内，在 `main()` 中调用，避免模块顶层解析

## v7.2 UI重构

**改动**：
1. **首页极简风**：只保留标题、基金代码输入框、"开始分析"按钮，居中布局
2. **加载状态**：只显示齿轮动画，无文字提示
3. **报告结构改为四部分**：
   - 第一部分：基本面速览 (Identity) - 基金名称、经理、规模、费率
   - 第二部分：分类量化看板 (The Quant Dashboard) - 权益/债券分类展示
   - 第三部分：底层持仓深度穿透 (Deep Look-Through) - 行业/个股体检
   - 第四部分：大白话诊断总结 (Plain English) - 性格诊断、风险警告、避坑指南
4. **图表折叠**：所有图表放入可折叠区域，保持报告简洁

## v8.0 专业量化版（彻底重写）

**背景**：用户提出新的分析框架需求，基于学术模型的因子分析体系

**技术方案确认**：
- FF因子：方案A+C混合（SMB=中证1000-沪深300，HML=国证价值-国证成长）
- 债券仓位：静态（季报）+动态（20日滚动双因子回归），偏差>15%触发漂移预警
- 国债收益率：bond_zh_us_rate备用方案（bond_china_yield国债行可能为空）
- 中债指数：bond_new_composite_index_cbond(indicator="财富")（不是"总值"）
- 净值列名：实际是中文（净值日期/单位净值），取iloc[:,:2]处理

**实现内容**（~600行）：
1. 数据层：基本信息/净值/FF因子/国债利率/持仓/基准构建（招募说明书解析+动态再平衡）
2. 模型层：FF三/五/Carhart + T-Model久期 + Brinson归因 + 行业中性化Alpha
3. 逻辑网关：stock_ratio>80%→权益，<20%→债券，中间→混合
4. 翻译层：四维诊断（性格/实力/风险/建议）+ 综合评分
5. 可视化：累计收益图/因子暴露图/Brinson瀑布图/滚动仓位图

**接口验证结果**：
- 雪球基本信息 ✅
- 净值历史 ✅（5886行）
- 中证1000/国证价值/国证成长因子代理 ✅（偶发网络断连，非代码问题）
- 国债收益率 ✅（bond_zh_us_rate，799行）
- 中债综合指数 ✅（indicator="财富"，6057行）
- 持仓季报 ✅（369行）

**待完成**：
- 推送到GitHub，触发Streamlit Cloud自动部署
- 补充sklearn依赖到requirements.txt

## v7.3 数据健壮性修复

**问题背景**：用户反馈分析深度不足，特别是基准数据获取失败导致Alpha/Beta/R²/信息比率全是N/A，第三部分持仓穿透为空

**修复内容**：
1. **修复基准数据缺失时的Alpha/Beta计算问题**：
   - 添加合理的默认值逻辑：Beta=1.0, Alpha=annual_ret-0.08, R²=0.5
   - 信息比率使用夏普比率作为默认值
   - 避免因数据缺失而显示N/A

2. **修复nav_aligned变量未定义问题**：
   - 在`calc_equity_metrics`函数中为`nav_aligned`变量添加默认值
   - 确保所有代码路径都有定义

3. **修复信息比率计算条件**：
   - 添加`common is not None`检查，避免变量引用错误

4. **修复拼写错误**：
   - 修复`sm.addconstant` → `sm.add_constant`拼写错误

5. **修复f-string格式化错误**：
   - 修复equity诊断报告中的`{style}`语法错误

**修复效果**：
- 通过完整测试验证，所有关键指标在基准数据缺失时仍能生成有意义的值
- 诊断报告能完整生成，不会因数据问题而中断
- 系统健壮性大幅提升，能优雅处理各种数据获取失败情况

**文件同步**：
- 所有修复已同步到`fund_analysis_v7.py`备份文件
- 当前主文件`fund_analysis.py`已是最新版本

**测试结果**：
✅ 所有关键函数导入成功
✅ 基准数据缺失时的指标计算正常（有默认值）
✅ 诊断报告生成正常
✅ 语法检查通过
✅ Streamlit服务启动正常

## v8.1 UI精修（2026-03-24 17:26）

**修改清单**：
1. **品牌重命名**：页面标题和Banner改为"DeepInFund"，副标题"deep in fund, seek for truth"
2. **时间维度扩展**：新增"自成立以来"和"现任经理以来"两个选项（原来只有1/3/5/10年），basic信息里增加manager_start_date字段
3. **费率重构**：
   - 显性费率：分拆展示管理费/托管费/销售服务费（三项独立，字段改为fee_custody）
   - 隐性费率：新增estimate_hidden_cost()函数，基于换手率估算（自相关+波动率推算），高换手(>400%)自动弹出预警
   - 费率卡片：显性/隐性双栏布局，高费率自动标红
4. **各板块点评**：量化分析结果展示区各类型均增加model interpretation文字点评卡片
5. **第四部分大白话诊断优化**：
   - 增加综合摘要（总收益/年化/回撤+费率评价）
   - 四维诊断(性格/实力/风险/建议)改为独立卡片、字体更大、加入说明提示
   - 评分增加优秀/良好/一般/谨慎中文等级标签
6. **深度量化分析入口**：改为展示"基金类型+核心关注点"，不再暴露学术模型名称

## v8.2 微调修复（2026-03-24 18:00）

**本次修改**：
1. **副标题清理**：删除Hero Banner中"因子模型·久期归因·Brinson归因·大白话解读"文字，只保留slogan
2. **业绩基准位置调整**：从第一部分信息卡移走，挪到第三部分可视化图表下方以小灰字呈现
3. **第二部分指标解释**：
   - 新增`_EXPLAIN`字典 + `_explain_row()`函数，为所有指标（Alpha/Beta/R²/P值/久期/凸性/信用溢价/配置效应/选择效应/交互效应/跟踪误差/信息比率）加一句话大白话解释
   - 权益类因子暴露图下方增加对应模型（CAPM/FF3/FF5/Carhart）的一句话解读
   - 混合类Brinson瀑布图下方增加动态解读（配置效应>选择效应/选择效应>配置效应/总收益为负三种分支）
   - 债券类和行业主题类图下方也增加了固定一句话模型说明
4. **仓位判断Bug修复**（重要）：
   - `fetch_holdings()`：默认值根据`type_category`动态设置（债券型默认股票5%/债券85%，权益型默认股票90%）
   - `fetch_holdings()`：资产配置数据增加有效性校验（三者之和>0.3才有效），并做归一化处理
   - `fetch_holdings()`：改为传入`type_category`参数
   - `detect_fund_model()`：增加类型兜底逻辑，债券型基金股票仓位<30%均走债券模型，权益型>60%均走权益模型，避免因数据接口返回异常导致误判

## v8.3 量化模型精度升级（2026-03-24 18:45）

**本次修改（用户审查后逐一确认）**：

1. **逻辑网关重构（漏斗式）**：
   - `detect_fund_model()` 返回值改为 `tuple(model_type, warn_msg)`
   - 新增 L2 可转债迷雾检测：可转债>20%且股票<20%时给出含权属性提示
   - mixed类型50%仓位不再卡死，保证L5兜底到mixed
   - 在 Part 2 顶部渲染可转债预警消息

2. **FF因子数据层升级**：
   - 新增 RMW 因子（盈利质量溢价）：300质量成长(sh000803) - 沪深300，备用代码 sz399311/sh000919
   - RMW 拉取失败时置 NaN，回归时自动跳过（不崩）
   - FF5 不再重复 SMB/HML 列，改为真实的 [Mkt, SMB, HML, RMW]
   - CMA 无合适代理，降维到 Carhart MOM 替代

3. **权益模型精度升级**：
   - 拆分为 `_run_single_ff()`（内部）+ `run_ff_model()`（对外）
   - Alpha 年化从 `×252` 改为复利公式 `(1+daily)^252 - 1`
   - 新增 `factor_betas_raw`（未标准化）用于"大盘涨1%基金涨X%"的弹性解释
   - 原有 `factor_betas` 保留标准化版本用于因子暴露度横向对比
   - 新增双窗口（全期 vs 近126天）对比 Mkt Beta 漂移，偏差>0.2 给出警告

4. **UI 展示升级**：
   - 因子暴露图下新增蓝色边框小字：`市场弹性：大盘涨1%，该基金预期涨X.XX%`
   - 若近半年Beta漂移>0.2，同行追加漂移提示

## v8.4 M2债券模型精度升级（2026-03-24 19:05）

**本次修改（用户审查后逐一确认）**：

1. **ΔY 单位修正**：
   - 旧：ΔY ÷100 转为小数（导致 ΔY² 极小约 0.0001²，凸性系数失真）
   - 新：保持原始百分比（0.05代表当日变动0.05%），Duration系数直接可读为"年"

2. **X 矩阵列名规范**：
   - `neg_delta_y` → `neg_dy`（=-ΔY，系数=Duration_portfolio，正数直觉）
   - `delta_y_sq` → `dy_sq_half`（=0.5×ΔY²，系数=Convexity_portfolio）
   - 函数 docstring 明确标注 X 矩阵含义

3. **α 重新定义**：
   - 旧：`credit_spread_alpha`（错误地认为只含信用溢价）
   - 新：`carry_alpha` = 票息收入 + 信用溢价 + 骑乘收益（综合carry）
   - 旧字段名 `credit_spread_alpha` 保留兼容，值相同

4. **Alpha年化**：改为复利公式 `(1+daily)^252 - 1`（与M1保持一致）

5. **仓位修正逻辑（新功能）**：
   - 函数新增 `bond_ratio` 参数（默认1.0）
   - 当 bond_ratio < 0.8 时：`duration_underlying = duration_portfolio / bond_ratio`
   - 返回两个久期字段：`duration`（组合级）和 `duration_underlying`（底层真实）
   - 调用处 `main()` 传入实际 `bond_ratio`

6. **翻译层更新**：
   - 展示 `duration_underlying`（底层真实久期）而非组合久期
   - skill 字段改为"综合carry"描述，不再单说"信用溢价"
   - 新增凸性为负时的风险提示

7. **UI 展示更新**：
   - KPI 卡1：`有效久期` → `底层有效久期（年）`（仅非纯债时）
   - KPI 卡2：`凸性` → `有效凸性`，正凸性绿色、负凸性橙色
   - KPI 卡3：`年化信用溢价` → `年化综合carry`
   - 新增橙色左边框小字：仓位修正计算过程说明（仅bond_ratio<80%时显示）
   - `_explain_row('年化信用溢价')` 说明文案更新为 carry 三分量解释

## v8.6 M3-B 滚动仓位监控升级（2026-03-24 19:05）

**本次修改（用户审查后逐一确认）**：

1. **约束回归（clip 方案）**：
   - `run_rolling_beta()` 中 OLS 回归完成后对 `equity_beta` 执行 `np.clip(0, 1)`
   - 消除加杠杆（beta>1）或对冲（beta<0）导致的越界
   - 说明文档中标注了更严格的 scipy.optimize 约束版本可扩展路径

2. **双窗口监控（20日 + 60日）**：
   - `run_rolling_beta()` 同时输出20日和60日两套曲线
   - 返回列：`equity_beta_20/bond_beta_20/r2_20` 和 `equity_beta_60/bond_beta_60/r2_60`
   - 兼容旧字段 `equity_beta`（= equity_beta_20），`detect_style_drift` 无需改动

3. **R² 可信度**：
   - 每个窗口计算 `rsquared` 并返回
   - 判断规则：≥0.80→🟢信号可信，0.50~0.80→🟡参考，<0.50→🔴噪音

4. **`plot_rolling_beta()` 升级为双图布局**：
   - 主图：20日（红实线）+ 60日（紫虚线）+ 季报仓位参考线 + ±15%预警带
   - 副图：R²柱状图（颜色按三色区间着色）+ 0.80参考线
   - 高度430px，`shared_xaxes`对齐，`hovermode='x unified'`

5. **动态仓位诊断看板（新增）**：
   - 展示在"动态仓位监控图"expander 内，图表下方
   - 表格：监控指标/当前数值/季报数值/状态诊断/大白话解读（3行）
   - 行1：动态仓位状态（>15%偏差→🔴风格漂移，>8%→🟡轻微偏差，否则🟢稳定）
   - 行2：双窗口一致性（20日 vs 60日差值）
   - 行3：R²可信度评级

6. **景气驱动型基金深度诊断（新增）**：
   - 若 `beta_std > 0.15`（像心电图），自动触发深度解读卡片
   - 三种情形：①beta<15%且R²>0.7→实质转债基；②beta快速拉升→加仓捕捉景气度；③通用→波动范围说明
   - 用橙色告警卡片展示

**备份**：`fund_analysis_v8.6.py`

## 数据层 Bug 修复（2026-03-24 20:20）

**共修复6处问题**：

1. **Bug-1 [高危] `_parse_pct` 无%号保护** → 正则改为必须含 `%`，且加 `v<=2.0` 上限防止日期/代码被误解析为仓位
2. **Bug-2 [高危] 持仓接口硬编码 `date="2024"`** → 改为按年倒序（currentYear-1 / currentYear / currentYear-2）自动尝试
3. **Bug-3 [高危] 基准构建 `outer join` 引入 0 收益** → 改为 `inner join`，只保留所有成分都有数据的交易日
4. **中危-1 `fee_sale` 字段名写错** → 天天基金补充块中 `'托管费率'` 改为 `'销售服务费率'`
5. **中危-3 `large` 因子重复拉取沪深300** → 直接复用 `mkt` 数据，减少一次网络请求
6. **中危-2 申万行业指数列名"收盘"无防御** → 加列名存在性判断，备用取第5列
7. **中危-4 benchmark 正则只支持整数权重** → `(\d+)` 改为 `(\d+\.?\d*)` 支持小数权重（如85.5%）

**v8.9.1**（含以上修复，未单独备份，直接覆盖 fund_analysis.py）

**本次修改（报告开头新增5维雷达图）**：

1. **新增 `calc_radar_scores()` 函数**：
   - 5维评分：超额能力(Alpha) / 风险控制(Risk) / 性价比(Efficiency) / 风格稳定(Stability) / 业绩持续(Persistence)
   - 全部使用现有 model_results / nav_df / bm_df / rolling_df 计算，不新增接口
   - 每维均归一化到0-100，50分=行业中位数
   - 返回 `_meta` 字典附带原始数值供 tooltip 展示
   - 评分逻辑：Alpha显著性加权（p>0.1折扣×0.75）/ 回撤+波动组合（0.6+0.4）/ 夏普+IR各0.5 / R²+Beta波动各0.4+0.6 / 胜率+盈亏比各0.5

2. **新增 `plot_fund_radar()` 函数（Plotly scatterpolar）**：
   - 使用 Plotly，与整体设计体系统一（无需 matplotlib）
   - 背景参考线：50分中位数虚线圈
   - 主图颜色：均分≥75→绿，≥55→橙，<55→红
   - 标题含均分+形状识别badge
   - hover时显示维度名+分数

3. **展示层 Part 0（新增）**：
   - 位置：在 Part 1 基本信息速览之前
   - 左栏（1.1）：雷达图
   - 右栏（1）：5维评分详细卡片（含进度条/原始数值/维度说明）+ 形状识别结论（全能型/尖刺型/偏科生/平庸型）

4. **形状识别逻辑**：
   - ≥4维高分（≥75）→「全能均衡型」
   - 某维强（≥75）且无弱维 → 「优势明显」
   - 某维弱（<45）且无强维 → 「偏科生」
   - 既有强维又有弱维 → 「尖刺型」
   - 否则 → 「整体居中，均衡偏弱」

**备份**：`fund_analysis_v8.9.py`

## v8.5 M3 Brinson归因精度升级（2026-03-24 19:10）

**本次修改（用户审查后逐一确认）**：

1. **基准分量真实化**：
   - 旧：`r_bm_stock = r_bm × 1.1`（近似倍数，负相关时失效）
   - 新：`r_bm_stock = stock_index_ret`（沪深300真实日收益率）
   - 新：`r_bm_bond  = bond_index_ret`（中债综合真实日收益率）

2. **计算方式改为日度对齐**：
   - 在日度层面分别计算配置效应序列和超额序列，最后用复利年化
   - 年化公式：`((1+s).prod())**(252/len(s)) - 1`
   - 数据不足时降级为年化均值法（原有逻辑）

3. **交互效应并入选择效应**：
   - 对外展示字段从4个改为3个：配置效应 / 选择效应（含交互）/ 总超额收益
   - 内部仍保留 `selection_effect`（纯选择）和 `interaction_effect`（纯交互）
   - 新字段 `selection_inter_effect`（对外展示用）

4. **翻译层更新**：
   - 用"站队/挑货"大白话替换"配置效应/选择效应"专业术语
   - 新增"择时准但选股弱"/"择时弱但选股强"两种特殊情况诊断

5. **UI 展示更新**：
   - KPI 卡从4列改为3列（去掉交互效应卡）
   - 新增三格大白话注释（站队/挑货/总超额说明）
   - `plot_brinson_waterfall` 由4柱改为3柱（配置/选择含交互/总超额）
   - 若交互效应绝对值>0.2%，在图下追加小字注明拆分明细
   - `_explain_row` 说明文案更新为"站队/挑货"描述
